import timeHelpers from '../../../utils/time-helpers';
import { module, test } from 'qunit';

module('timeHelpers');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = timeHelpers();
  assert.ok(result);
});
