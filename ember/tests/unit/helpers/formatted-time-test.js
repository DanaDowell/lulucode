import {
  formattedTime
} from '../../../helpers/formatted-time';
import { module, test } from 'qunit';

module('FormattedTimeHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formattedTime(42);
  assert.ok(result);
});
