import Ember from 'ember';

export default Ember.ArrayController.extend({
  /*queryParams: ['weekday'],
  weekday: null,

  filteredMarkets: function() {
    var weekday = this.get('weekday');
    var markets = this.get('model');

    if (weekday) {
      return markets.filterBy('weekday', weekday);
    } else {
      return markets;
    }
  }.property('weekday','model')*/


});
